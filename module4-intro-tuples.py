empty_tuple = ()
one_el_tuple_a = (1,)
one_el_tuple_b = 1,
three_el_tuple = 1, 2, 3,
print(three_el_tuple)

# Tuples can have multiple datatypes
# Lists have [] brackets, Tuples have () brackets
# Main diffrence is Mutability
# two kinds of data, mutable (can be updated, add new values like append) and immutable (you cannot add new values, only replace them)

